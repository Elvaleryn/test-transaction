### Installation

- ``` yarn install ``` to install all the dependencies
- ``` yarn dev ``` to start the server
- ``` yarn test ``` to run tests

### What it does

It takes an array of transactions and calculate balances of each user.
